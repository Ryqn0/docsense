apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: docsense
spec:
  selector:
    app: frontend
  ports:
    - port: 8501
      targetPort: 8501
  type: LoadBalancer      # was ClusterIP - gives a real public IP
