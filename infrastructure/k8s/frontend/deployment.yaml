apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: docsense
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: europe-west9-docker.pkg.dev/docsense-496900/docsense/frontend:latest
               # docsense-frontend:local
          imagePullPolicy: Always    # Never
          ports:
            - containerPort: 8501
          env:
            - name: API_URL
              value: "http://api:8000"
